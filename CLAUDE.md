See @AGENTS.md for project instructions and conventions.

When asked to save a plan put it in the `plans/` directory in the root of this repository.

# Bug fixes

Before fixing a bug, write a failing test that reproduces it. The test should fail against the current code and pass after the fix is applied.
